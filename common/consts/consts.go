package consts

const (
	HpcJudgeDiscoveryType  = "hpc-judge"
	HpcBridgeDiscoveryType = "hpc-bridge"
)

const (
	RunCommandReportFile = "run-command-report.json"
	JudgeReportFile      = "result.json"
)

const SolutionFileName = "solution.dat"
